# 前端搭建完成后待办提醒

前端项目搭好、页面能跑通之后，请按下面清单检查并完成这些事项。

---

## 1. 体型参考图（必做）

**接口**：`GET /api/body-profile/options` 返回 `{ female: [...], male: [...] }`，每项带 `value`、`label`、`imageUrl`（占位路径）。

**女生体型**（5 种）：梨型、倒三角型、沙漏型、H型、苹果型（图片路径已按中文文件名配置，放入即显示）

| 体型 label | value | 文件名（放入 `public/images/body-ref/female/`） |
|------------|--------|------------------------------------------------|
| 梨型 | pear | `梨型.png` |
| 倒三角型 | inv_triangle_f | `倒三角型.png` |
| 沙漏型 | hourglass | `沙漏型.png` |
| H型 | h_type | `H型.png` |
| 苹果型 | apple | `苹果型.png` |

**男生体型**（5 种）：O型、正三角型、矩形、倒三角型、倒梯型

| 体型 label | value | 占位路径 |
|------------|--------|----------|
| O型 | o_type | `/images/body-ref/male/o_type.png` |
| 正三角型 | triangle | `/images/body-ref/male/triangle.png` |
| 矩形 | rectangle | `/images/body-ref/male/rectangle.png` |
| 倒三角型 | inv_triangle_m | `/images/body-ref/male/inv_triangle.png` |
| 倒梯型 | inv_trapezoid | `/images/body-ref/male/inv_trapezoid.png` |

**你需要做的**：

- 在 `frontend/public/images/body-ref/` 下建立 `female/` 与 `male/` 两个子目录。
- 将上述 10 张体型参考图放入对应路径（文件名与上表一致）。
- 若使用其他目录或 CDN，在调用体型选项、展示参考图的地方改用你的路径（或保证接口返回的 `imageUrl` 可访问）。

**使用这些图的页面**：虚拟试衣页（体型与身体参数）、「我的」- 体型与身体数据。两处会根据**性别**展示对应体型选项与占位图。

---

后续若有其他「搭建后再做」的待办，可继续写在本文件下面。
